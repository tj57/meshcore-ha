# Vendored mcRPC Python package

Copy of `mcrpc/python/mcrpc` from https://github.com/tj57/mcrpc @ v1.0.0-line.

Used as a fallback when the pip requirement cannot install (PyPI hosts an
unrelated `mcrpc` package). Keep in sync when the protocol library changes.
